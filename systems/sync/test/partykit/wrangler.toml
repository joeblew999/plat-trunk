name = "sync-partykit-test"
main = "worker.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

# DO binding name "MAIN" maps to URL /parties/main/:room via hono-party
[durable_objects]
bindings = [
  { name = "MAIN", class_name = "Main" }
]

[[migrations]]
tag = "v1"
new_classes = ["Main"]

[dev]
port = 1999
