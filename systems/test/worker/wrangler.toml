# Test Worker — minimal worker for validating cf-deploy N-worker topology
name = "test-worker"
main = "src/index.ts"
compatibility_date = "2025-10-08"
compatibility_flags = ["nodejs_compat"]
workers_dev = true
preview_urls = true

[dev]
ip = "0.0.0.0"
port = 5175
local_protocol = "http"
