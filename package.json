{
  "scripts": {
    "dev": "node run.mjs dev",
    "build": "bun run build:truck && bun run build:docs",
    "build:truck": "cd systems/truck/crate && wasm-pack build --target web --release --out-dir ../web/pkg-browser-renderer && cargo run --bin generate-schema 2>/dev/null > ../cad-schema.json",
    "build:docs": "cd systems/docs/website && bun install --silent && bun run build",
    "test": "bun run test:crate && bun run test:api",
    "test:crate": "cd systems/truck/crate && cargo test",
    "test:api": "cd systems/truck/worker && bun x vitest run",
    "test:api:watch": "cd systems/truck/worker && bun x vitest watch",
    "test:e2e": "cd systems/truck/tests && bun x playwright test",
    "deploy": "node run.mjs deploy",
    "deploy:nuke": "bun scripts/cf-deploy.ts nuke",
    "deploy:upload": "bun scripts/cf-deploy.ts upload"
  },
  "dependencies": {
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260212.0",
    "wrangler": "^4.69.0"
  }
}
